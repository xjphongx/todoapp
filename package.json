{
  "name": "deployed-to-do-app",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "if-env TYPE=server && (cd server && npm install) || (cd client && npm install)",
    "server": "cd server && npm run start",
    "client": "cd client && npm start",
    "extra": "nodemon ./server/server.mjs"

  },
  "author": "",
  "dependencies": {
    "if-env": "^1.0.4",
    "nodemon": "^3.0.1"
  }
}
